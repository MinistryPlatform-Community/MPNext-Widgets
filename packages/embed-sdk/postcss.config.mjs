// Empty PostCSS config — the embed SDK does not use Tailwind/PostCSS.
// This file prevents Vite from inheriting the root postcss.config.mjs.
const config = {
  plugins: [],
};

export default config;
